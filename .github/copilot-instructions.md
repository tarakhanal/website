# Copilot Instructions

- This is a static wedding website. There is no need for SSR (Server Side Rendering). Use `output: "export"` compatible patterns only.
- The primary audience uses **iPhone with Safari**. All features must work correctly on iOS Safari. Desktop should also be supported.
- UI is important — everything must look **aesthetic and appealing**.
- **Never commit any git changes.**
